--- libdbus-c++-0.9.0/include/dbus-c++/dispatcher.h.orig  2011-11-28 17:03:19.000000000 +0200 
+++ libdbus-c++-0.9.0/include/dbus-c++/dispatcher.h       2017-03-28 11:13:28.571991579 +0200 
@@ -267,6 +267,19 @@ struct Threading 
      return new Mx; 
    } 

+#ifndef DBUS_HAS_RECURSIVE_MUTEX 
+  static bool mutex_free(Mutex *mx) 
+  { 
+    delete mx; 
+    return true; 
+  } 
+ 
+  static bool mutex_lock(Mutex *mx) 
+  { 
+    mx->lock(); 
+    return true; 
+  } 
+#else 
    static void mutex_free(Mutex *mx) 
    { 
      delete mx; 
@@ -276,6 +289,7 @@ struct Threading 
    { 
      mx->lock(); 
    } 
+#endif 

    static void mutex_unlock(Mutex *mx) 
    { 

